# local DB credentials — never commit
db_url.txt
.env

# Kaggle API token — never commit
kaggle.json
.kaggle/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# generated artifacts (rebuilt from Postgres on demand)
results/
outputs/
uploads/
checkpoints/
payload.json
*.log

# local data / caches (mounted volumes in compose)
backups/
test_data/
data/
models/

# debug: downloaded kaggle kernel output
_kaggle_out/

# build artifacts / deliverables
*.zip
