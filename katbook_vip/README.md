# katbook_vip/ — pipeline domain

The ML/processing core: voice/silent router, ingest, audio (Whisper), visual
(CLIP/YOLO/OCR/BLIP-2), NLP, segmentation, LLM tagging, embeddings, storage, and
export. Imported by the worker; also runnable as a batch CLI (`python -m katbook_vip`).
