# docs/ — documentation

Index of project docs:
- **DEPLOYMENT.md** — RTX 5090 runbook + top-10 troubleshooting.
- **API.md** — every endpoint + curl, the CORS / X-API-Key contract (for the frontend).
- **DATABASE.md** — schema (mermaid), indexes, capacity math, backup/restore.
- **ARCHITECTURE.md** — service diagram, POC→prod changes, preserved safety rules.
- **PLAN.md** — the original transformation plan (file manifest).
- **CLEANUP.md** — the dependency audit behind the cleanup pass.
