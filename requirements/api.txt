# API service deps. NO torch / transformers / ML — the API stays lightweight.
-r base.txt
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20     # multipart uploads to the inbox
httpx==0.28.1
openai==1.99.1               # OpenAI-compatible embeddings client (query embedding only)
