# api/ — API domain

FastAPI service (routers, schemas, services, deps, middleware). A lightweight,
versioned REST surface over Postgres + the Celery queue — **loads no ML model and
serves no UI**. Entry point: `api.main:app`.
